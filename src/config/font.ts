import { Hind_Siliguri, Kumbh_Sans } from '@next/font/google';

// const display = Halant({ subsets: ['latin'], weight: ['400', '500', '700'] });
const display = Kumbh_Sans({ subsets: ['latin'], weight: ['400', '500', '700'] });
const body = Hind_Siliguri({ subsets: ['latin'], weight: ['400'] });
// const body = Kumbh_Sans({ subsets: ['latin'], weight: ['400'] });

export { body, display };
