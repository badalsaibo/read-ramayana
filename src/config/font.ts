import { Hind_Siliguri, Gantari } from '@next/font/google';

const display = Gantari({ subsets: ['latin'], weight: ['800'] });
const body = Hind_Siliguri({ subsets: ['latin'], weight: ['300'] });

export { body, display };
