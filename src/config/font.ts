import { Hind_Siliguri, Gantari, Kumbh_Sans } from '@next/font/google';

const display = Kumbh_Sans({ subsets: ['latin'], weight: ['800'] });
const body = Hind_Siliguri({ subsets: ['latin'], weight: ['300'] });

export { body, display };
