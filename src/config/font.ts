import { Hind_Siliguri, Kumbh_Sans } from '@next/font/google';

const display = Kumbh_Sans({ subsets: ['latin'], weight: ['500', '700'] });
const body = Hind_Siliguri({ subsets: ['latin'], weight: ['300'] });

export { body, display };
