import { Hind_Siliguri, Kumbh_Sans } from '@next/font/google';

const body = Hind_Siliguri({ subsets: ['latin'], weight: ['400'], display: 'swap' });
const display = Kumbh_Sans({ subsets: ['latin'], weight: ['400', '500', '700'], display: 'swap' });

export { body, display };
